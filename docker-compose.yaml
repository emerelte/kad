version: '3.8'
services:
  kad:
    container_name: "kad"
    build: kad/.
    network_mode: "host" # FIXME - unsafe
    privileged: true
  load-test:
    container_name: "load-test"
    build: load_test/.
    network_mode: "host"
    privileged: true

